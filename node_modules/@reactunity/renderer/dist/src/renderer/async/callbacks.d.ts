import { AsyncCallback } from './commands';
import { ObjectsRepo } from './objects';
export declare class CallbacksRepo extends ObjectsRepo<AsyncCallback> {
    call: (ind: number, args: any[]) => any;
}
